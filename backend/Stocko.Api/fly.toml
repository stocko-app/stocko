app = 'stocko-app-api'
primary_region = 'ams'

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  # /health testa Postgres (timeout 4s na app). Se a BD morrer, a Fly reinicia a máquina.
  [[http_service.checks]]
    interval = "30s"
    timeout = "6s"
    grace_period = "20s"
    method = "GET"
    path = "/health"

[[vm]]
  memory = '256mb'
  cpus = 1
  memory_mb = 256